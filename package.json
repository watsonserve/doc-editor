{
  "name": "doc-editor",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["@watsonserve/*"],
  "scripts": {
    "up": "yarn workspace @watsonserve/editor build",
    "start": "yarn workspace @watsonserve/doc start",
    "build": "yarn up && yarn workspace @watsonserve/doc build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
